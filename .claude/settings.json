{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // .tool_response.filePath' | { read -r f; if echo \"$f\" | grep -qE '\\.sol$'; then forge fmt \"$f\"; fi; } 2>/dev/null || true",
            "statusMessage": "Running forge fmt..."
          }
        ]
      }
    ]
  }
}
